# Example environment file for holo_mesh_node.service
# Copy to /etc/default/holo_mesh_node and edit.

HXN_BIND=0.0.0.0:5001
HXN_STORE_ROOT=/opt/holographix/cortex_node
HXN_RATE_HZ=40
HXN_FORWARD_PROB=0.25
HXN_MAX_PAYLOAD=1200

# Peers (space-separated --peer flags)
HXN_PEER_OPTS="--peer 10.0.0.2:5001 --peer 10.0.0.3:5001"

# Optional auth (32-byte hex key)
HXN_AUTH_OPTS="--auth-key deadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef"

# Optional seeding of existing content into the store
# HXN_SEED_OPTS="--seed-uri holo://demo/flower --seed-chunk-dir /opt/holographix/data/flower.jpg.holo"

# Extra flags (e.g., packet budget, different coarse side)
# HXN_EXTRA_OPTS="--packet-bytes 1136 --coarse-side 16"
