# Example environment file for holo_mesh_receiver.service
# Copy to /etc/default/holo_mesh_receiver and edit.

HXR_LISTEN=0.0.0.0:5000
HXR_OUT_DIR=/opt/holographix/cortex_rx
HXR_DECODE=/opt/holographix/out/recon.png
HXR_DURATION=3600
HXR_MAX_PAYLOAD=1200

# Optional auth (32-byte hex key)
# HXR_AUTH_OPTS="--auth-key deadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef"

# Extra flags (e.g., packet budget)
# HXR_EXTRA_OPTS="--max-payload 1200"
