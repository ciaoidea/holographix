# Example environment file for holo_mesh_sender.service
# Copy to /etc/default/holo_mesh_sender and edit.

HXS_URI=holo://demo/flower
HXS_CHUNK_DIR=/opt/holographix/data/flower.jpg.holo
HXS_PEER=10.10.3.2:5000
HXS_MAX_PAYLOAD=1200
HXS_REPEATS=2

# Optional auth (32-byte hex key)
# HXS_AUTH_OPTS="--auth-key deadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef"

# Extra flags (e.g., packet budget)
# HXS_EXTRA_OPTS="--max-payload 1200"
